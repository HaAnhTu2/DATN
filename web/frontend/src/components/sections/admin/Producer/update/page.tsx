
interface UpdateProducerProps {
    id: string;
}

export default function ProducerUpdate({ id }: UpdateProducerProps) {

    return (
        <div>
            ProducerUpdate
        </div>
    );
};
